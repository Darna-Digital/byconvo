{
  "name": "@codediff/core",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "codediff core — Effect backend for git and GitHub",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@effect/platform-node": "4.0.0-beta.82",
    "effect": "4.0.0-beta.82"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
